import { countDown } from "../../utils/countDown.js";

describe("tests for countDown function", () => {
   

 it ('should operate correctly with number  ', () => {
     expect(countDown(even number)).toBe('Number${even number} is an even number');
    }),
 it('should operate correctly with ', () => {
     expect(countDown(negative number)).toBe('Number${negative number} is a negative number');
 }),
 it('should operate correctly with invalid data', () => {
     expect(countDown('love')).toBe('Data is invalid);
  }),    
})